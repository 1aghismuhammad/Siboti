services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: laravel-apache-app
    restart: unless-stopped
    ports:
      - "8000:80"
    volumes:
      - .:/var/www/html
    networks:
      - laravel-network

networks:
  laravel-network:
    driver: bridge